package com.example.vehicleequalizernative

/**
 * Configuração global da aplicação.
 * Substitui o uso do BuildConfig para debug/produção.
 */
object AppConfig {
    // Troque para false em produção, ou faça leitura via variável de ambiente/flag
    const val DEBUG: Boolean = true
}
